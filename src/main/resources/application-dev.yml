server:
  port: 8085
db:
  port: 3306
  name: developer_mobilestore_fix
  endpoint: localhost
  username: root
  password: 15022002


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${db.endpoint}:${db.port}/${db.name}
    username: ${db.username}
    password: ${db.password}
url:
  redirect:
    path: ${domain.path}
domain:
  path: http://localhost:3000
